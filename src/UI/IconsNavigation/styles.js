import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  root: {
    width: 63,
  },
  button: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: 48,
    borderRadius: 0,
    fontSize: 18,
  },
});
