import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  root: {
    minHeight: 54,
    display: 'flex',
    padding: '0 25px',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
});
