import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  header: {
    display: 'flex',
    justifyContent: 'space-between',
  },
  content: {
    marginTop: 13,
  },
});
