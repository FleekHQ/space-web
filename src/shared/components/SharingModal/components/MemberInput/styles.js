import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  root: {
    display: 'flex',
    alignItems: 'center',
  },
  input: {
    padding: 0,
  },
  inputRoot: {
    flex: 1,
    display: 'flex',
    margin: '0 18px',
  },
});
