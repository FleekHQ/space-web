import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  root: {
    display: 'flex',
    alignItems: 'center',
  },
  autocomplete: {
    margin: '0 18px',
    width: '100%',
  },
  textfield: {
    width: '100%',
  },
});
