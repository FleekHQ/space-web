import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  root: {
    flexGrow: 1,
    display: 'flex',
  },
  viewContent: {
    display: 'flex',
    position: 'relative',
    flexDirection: 'column',
  },
});
